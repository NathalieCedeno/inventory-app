pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Compilando el proyecto...'
                sh 'mvn clean compile'
            }
        }
        stage('Test') {
            steps {
                echo 'Ejecutando pruebas unitarias...'
                sh 'mvn test'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Simulando despliegue del proyecto...'
                echo 'Aplicación desplegada correctamente en el entorno de prueba.'
            }
        }
    }
}
